const Employee = require("./Employee");

class Intern {
    constructor(school){
        super(employeeName, id, emailAddress, officeNumber);
        this.school = school;
    }
}
module.exports = Intern;